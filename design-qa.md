# 视觉 QA

## 2026-06-09 回归洁白轻盈版本

- 参考目标: 用户要求停止继续尝试各种风格，回到最早那版洁白、轻盈、透气的 Floating Todo。
- 已检查: 原生窗口级截图中主窗口恢复为白色玻璃外壳、白色内层纸面、浅金右侧书签和轻投影，不再是蓝底手绘或黑白复古风格。
- 已检查: 标题默认是纯文本展示，未再出现灰色输入框选中块；双击标题仍可编辑。
- 已验证: `swift build`、`npm run build`、`/Applications/FloatingTodo.app` 安装后窗口级截图检查通过。
- 剩余偏差: 标题、页签和任务内容来自用户本地数据，因此截图显示当前用户正在使用的 CMI MAP 页面。

final result: passed

## 2026-06-09 蓝底手绘书签风格返工

- 参考目标: 用户提供的蓝底白字咖啡海报、手绘 todo checklist 和侧边书签草图；重点是蓝色主底、白色手绘线、右侧彩色书签、弱投影和页面底色切换。
- 已检查: Web 预览截图中主面板已从硬网页改为蓝底手绘 notebook，右侧书签外凸，白色手绘边框、输入框和任务圆点均可见。
- 已检查: 原生窗口级截图中当前页为红色底，右侧保留蓝/绿/红/白加号页签，证明页面色可随真实页面索引变化；长页面名在书签中被短名处理，不再挤爆。
- 已检查: 原生输入框 placeholder 已改为白色手绘文字，不再出现深色系统 placeholder。
- 已验证: `swift build`、`npm run build`、`/Applications/FloatingTodo.app` 安装后窗口级截图和 Web 预览截图检查通过。
- 剩余偏差: 真实任务和页面标题来自用户本地数据，当前截图显示红色页是因为本地激活页为第 3 个页面；切回第 1 页会显示蓝色底。

final result: passed

## 2026-06-09 旧网页复印纸风格回收

- 参考目标: 用户最新提供的 NEW YEAR 旧网页截图，重点是暖米白背景、近黑粗线、日期+横线+三圆点浏览器栏、超粗标题、大号单选框、厚边按钮和弱硬影。
- 已检查: 原生窗口级截图中不再是三列卡片网格；标题、BOOKMARKS、TASKS 和输入栏仍保持上一版单栏便贴顺序。
- 已检查: 原生窗口的黑色投影已从大块硬影降成弱偏移；纸面噪点和扫描线存在但不糊，控件依靠粗黑线和黑底按钮建立反差。
- 已检查: Web 预览截图与原生顺序一致，未保留上一轮“习惯打卡网格”布局。
- 已验证: `swift build`、`npm run build`、`/Applications/FloatingTodo.app` 安装后窗口级截图和 Web 预览截图检查通过。
- 剩余偏差: 实际便贴标题、页签和任务内容来自用户本地数据，不强行改成参考图中的 NEW YEAR 示例文案。

final result: passed

## 2026-06-09 习惯打卡网格风格重做

- 参考目标: 用户提供的习惯打卡 App 截图，重点是白底、黑色粗标题、日历图标、圆形日期 chip、绿色完成态、进度行、三列任务卡片和底部线性插画。
- 已检查: 原生窗口级截图中标题、日期 chip、0/3 进度、问号/设置方块、三列网格、浅色卡片背景、黑色线性图标和绿色添加按钮均已落位。
- 已检查: 当前只有 3 个真实任务时，空白格仍保留网格边界，不再出现大块无结构空白。
- 已验证: `swift build`、`npm run build` 和 `/Applications/FloatingTodo.app` 安装后窗口级截图检查通过。
- 剩余偏差: 图标使用系统/Lucide 线性图标近似参考插画；真实标题、页签和任务内容来自用户本地数据。

final result: passed

## 2026-06-08 复古浏览器小报风格重做

- 参考目标: 复古浏览器窗口、黑白高对比、米白纸张底色、粗体无衬线标题、低分辨率印刷模糊感、厚描边按钮和表单控件视觉化。
- 已检查: 原生窗口级截图中顶部浏览器栏、日期、圆形控制点、巨大粗体标题、粗 radio 页选择、黑底硬阴影按钮和放大鼠标指针均已落位。
- 已检查: 整体只使用米白/近黑，去掉彩色点缀、柔光、渐变和现代 SaaS 卡片感；任务区不再露出半截控件。
- 已验证: `swift build`、`npm run build` 和 `/Applications/FloatingTodo.app` 安装后窗口级截图检查通过。
- 剩余偏差: 日期显示来自系统运行时日期；实际便贴标题和任务内容来自用户当前数据。

final result: passed

## 2026-06-08 手绘 planner 风格返工

- 参考目标: 用户最后提供的米白手绘 planner 图，重点是纸面底色、黑色手绘线、方形 checkbox、轻量手写标题和朴素分区。
- 已检查: 原生窗口级截图中不再有玻璃拟物、金色纸签、星标和标签胶囊；主面板改为米白纸面，待办行是黑色手绘方框，右侧是小号米色纸签。
- 已检查: 标题默认展示不再露出 TextField 灰色编辑底，底部加号缩小到更接近参考图的手绘控件比例。
- 已验证: `swift build`、`npm run build` 和 `/Applications/FloatingTodo.app` 安装后窗口级截图检查通过。
- 剩余偏差: 实际内容来自用户当前便贴数据，不强行改成参考图里的示例模块。

final result: passed

## 2026-06-05 参考图级 UI 重写

- 参考目标: Image Gen 的玻璃浮窗、内层纸面、右侧大索引签。
- 已检查: 桌面截图中主窗口、顶部 chrome、内层纸面和右侧纸签层级已对齐；390px 窄屏截图无不可用横向溢出。
- 已验证: 右侧加号可新建便贴，新增便贴后可切回原便贴。
- 剩余偏差: 实际待办内容来自用户数据，不强行改成参考图里的示例任务。

final result: passed

## 2026-06-05 真实原生截图校准

- 已检查: 真实 `/Applications/FloatingTodo.app` 截图中背景网页不再穿透到纸面，窗口硬黑边已去掉。
- 已检查: 右侧纸签为「待办 / 灵感 / +」，和参考图结构一致。
- 已验证: 当前运行进程来自 `/Applications/FloatingTodo.app/Contents/MacOS/FloatingTodo`。

final result: passed

## 2026-06-05 参考图 1:1 复刻返工

- 已检查: 桌面截图中外层玻璃窗、内层纸面、三张右侧纸签、五行任务、分类胶囊、星标和输入胶囊均按参考图结构落位。
- 已检查: 390px 窄屏截图中五行任务、三张纸签和底部输入栏完整可见。
- 已验证: 只有一个便贴页时显示“灵感”纸签，点击后创建并切换到真实“灵感”页。

final result: passed

## 2026-06-05 尺寸收敛

- 已检查: 桌面截图中主窗从 532×520 视觉体量缩到 460×448，右侧纸签仍贴在纸边外侧。
- 已检查: 390px 窄屏截图中主纸面、输入栏和两个纸签可见。
- 已验证: 右侧加号仍可新建便贴。

final result: passed
